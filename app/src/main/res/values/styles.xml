<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/loginpurple</item>
        <item name="colorPrimaryDark">@color/background</item>
        <item name="colorAccent">@color/lightpurple</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="NoteTitlesListTheme" parent="@android:style/Widget.ListView">
        <item name="android:textColor">#000000</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="loginScreenField" parent="Theme.AppCompat.Light.DarkActionBar">

        <item name="colorControlActivated">@color/loginpurple</item>
        <item name="colorControlHighlight">@color/loginpurple</item>
    </style>

    <style name="buttonStyle" parent="@android:style/Widget.Button">
        <item name="android:background">@drawable/custom_button</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textAppearance">@style/Base.TextAppearance.AppCompat.Large</item>
        <item name="android:textSize">24sp</item>
        <item name="android:padding">10dp</item>
        <item name="android:layout_margin">10dp</item>
        <item name="android:minHeight">0dp</item>
        <item name="android:minWidth">0dp</item>
    </style>

    <style name="radioButtonTheme" parent="@android:style/Widget.Holo.CompoundButton.RadioButton">
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="dialogStyle" parent="@android:style/Widget.ListView">
        <item name="android:textColor">@color/white</item>
        <item name="android:background">@color/background</item>
        <item name="android:buttonStyle">@style/buttonStyle</item>
        <item name="android:radioButtonStyle">@style/radioButtonTheme</item>
        <item name="android:textAppearance">@style/TextAppearance.AppCompat.Large</item>
    </style>

    <style name="menuStyle" parent="@android:style/Widget.ListView">
        <item name="android:textColor">@color/white</item>
        <item name="android:background">@color/background</item>
    </style>

    <style name="listViewStyle">
        <item name="android:padding">50dp</item>
    </style>

</resources>
